<script is:inline>
  function setup() {
    createCanvas(windowWidth, windowHeight);

    background(50);

    noFill();
    stroke(230);
    circle(width / 2, height / 2, 55);
  }

  function draw() {
  }
</script>
