// import GUI from "lil-gui";
// const gui = new GUI();
// export default gui;

// export function getController(propName) {
//   for (const folder of gui.folders) {
//     const controller = folder.controllers.find(
//       (ctrl) => ctrl.property === propName
//     );
//     if (controller) return controller;
//   }
//   return gui.controllers.find((ctrl) => ctrl.property === propName);
// }
